/**
 * @class IextradingController
 */
const _ = require('lodash');
const request = require('request');

/**
 * With Iextrading we can do amazing things, like loading the last 100000 news of the market every n minutes and see
 * if any of them is an FDA approval or matches any particular word and right there trigger an alert
 * @type {{}}
 */

module.exports = {

};
